{
  "name": "evalright-workspace",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client/frontend",
    "build": "npm run build --prefix client/frontend",
    "install-all": "npm install --prefix client/frontend"
  }
}
